/**
 * Svg-иконка
 *
 * value {String}
 * appendIcon
 * prependIcon
 * clearable {Boolean}
 * maxLength {Number}
 * placeholder {String}
 * counter {Boolean}
 * ...otherAttrs
 */

import model from "./model";
import template from "./template";
import "./style";

export { model, template };
