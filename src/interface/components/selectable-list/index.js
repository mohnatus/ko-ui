/**
 * Список
 *
 * items[ id, text, level, disabled ]
 * selected {string|number|Array}
 * disabled[]
 * onSelect {Function}
 * itemComponentName {String}
 */

import model from "./model";
import template from "./template";
import "./style";

export { model, template };
