/**
 * Селект
 * multiple {Boolean}
 * value {String|Array}
 * disabled {Boolean}
 * options[]
 * clearable {Boolean}
 * searchable {Boolean}
 * loading {Boolean}
 */

import model from "./model";

import template from "./template";

import "./style";

export { model, template };
