/**
 * Кнопка
 *
 * icon {Object.<name, width, height, size, color>}
 * appendIcon {Object.<name, width, height, size, color>}
 * prependIcon {Object.<name, width, height, size, color>}
 * onPress {Function}
 * as {'link'}
 * href {String}
 * linkAttrs {Object}
 * disabled {Boolean}
 * size {String}
 * theme {String}
 * mode {String}
 */

import model from "./model";
import template from "./template";
import "./style";

export { model, template };
